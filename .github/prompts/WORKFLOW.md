# 管理者用プロンプト：ブランチ・コミット・プッシュワークフロー

このドキュメントは、各管理者用プロンプト（`.admin.prompt.md`）で使用される共通のGitワークフローを定義しています。

## 基本ワークフロー

各管理者用プロンプトは、以下の基本的なGitワークフローに従います：

### 1. ブランチの作成

作業を開始する前に、各プロンプト専用のブランチを作成します。

```bash
git checkout -b <ブランチ名>
```

各プロンプトで使用されるブランチ名：

| プロンプト | ブランチ名 |
|----------|-----------|
| `/topics.admin` | _(ブランチ作成不要、mainで直接作業)_ |
| `/suggest-scenario.admin` | `feature/suggest-scenarios` |
| `/plan.admin` | `feature/make-a-plan` |
| `/readme.admin` | `feature/make-readme` |
| `/tutorial.admin` | `feature/make-tutorial` |
| `/implement-test.admin` | `feature/implement-tests` |
| `/verify.admin` | `feature/verify-assignment` |
| `/release.admin` | `feature/remove-admin-prompts` |

### 2. 変更の確認とコミット

作業が完了したら、変更内容を確認し、適切なコミットメッセージでコミットします。

```bash
git status
git diff
git add .
git commit -m "<コミットメッセージ>"
```

#### コミットメッセージのプレフィックス

- `add:` - 新しいファイルやコンテンツを追加した場合
- `feat:` - 新機能を追加した場合
- `fix:` - 修正を行った場合
- `update:` - 既存の内容を更新した場合

### 3. リモートリポジトリへのプッシュ

コミット後、作業ブランチをリモートリポジトリにプッシュします。

```bash
git push origin <ブランチ名>
```

### 4. プルリクエストの作成

GitHubのウェブインターフェースでプルリクエストを作成し、レビューを依頼します。

---

## 各プロンプトでの参照方法

各管理者用プロンプトでは、具体的なブランチ名とコミットメッセージを指定し、本ドキュメントを参照してください。

**参照例：**

```markdown
## 手順

1. [共通ワークフロー](./WORKFLOW.md) に従い、ブランチ `feature/XXX` を作成してください。

2. （プロンプト固有の作業手順）

3. [共通ワークフロー](./WORKFLOW.md) に従い、変更をコミット（メッセージ: "XXX"）し、リモートリポジトリにプッシュしてください。
```

---

## 注意事項

- ブランチ名は各プロンプトで一貫性を保つため、上記の命名規則に従ってください。
- コミット前には必ず `git status` と `git diff` で変更内容を確認してください。
- 不要なファイル（ビルド成果物、一時ファイルなど）がコミットされないよう注意してください。
- リモートへのプッシュ後、必ずプルリクエストを作成し、レビュープロセスを経てください。

---

## レビュー後の修正プロセス

プルリクエスト作成後、教員のレビューを受けます。レビュー結果に応じて以下のいずれかの対応を行います。

### ケース1: 軽微な修正が必要な場合

**状況:** タイポの修正、コメントの追加、フォーマットの調整など

**対応:**
```bash
# 同じブランチで修正を継続
git checkout <ブランチ名>  # 例: feature/make-a-plan
# ファイルを編集
git add .
git commit -m "fix: レビュー指摘事項の修正"
git push origin <ブランチ名>
```

レビュー指摘事項を修正後、同じプルリクエストで再度レビューを依頼します。

### ケース2: 大幅な修正が必要な場合（方向性の変更）

**状況:** 課題の方向性が間違っている、テストシナリオの大幅な再構成が必要など

**対応:**
```bash
# 現在のブランチとプルリクエストをクローズ
git checkout main
git branch -D <ブランチ名>

# 必要に応じて入力ファイルを修正
# 例: agent-input/topics.md や agent-output/plan.md を編集

# プロンプトを再実行（新しいブランチが自動作成される）
```

### ケース3: 前のステップに戻る必要がある場合

**状況:** `/verify.admin` で問題が見つかり、特定のステップに戻る必要がある場合

**対応:** [REVIEW_FLOW.md](../../REVIEW_FLOW.md) の「/verify.admin の問題種別と対処ステップ」を参照してください。

詳細なレビューフロー、問題種別ごとの対処方法については [REVIEW_FLOW.md](../../REVIEW_FLOW.md) を参照してください。
