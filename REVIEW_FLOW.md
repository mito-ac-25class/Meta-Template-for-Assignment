# レビューフローと修正プロセス

このドキュメントは、課題リポジトリ作成プロセスにおける各ステップのレビューフロー、修正手順、および問題発生時の対処方法を定義します。

## 目次

- [基本的なレビューフロー](#基本的なレビューフロー)
- [各ステップのレビューと承認フロー](#各ステップのレビューと承認フロー)
- [verify.admin の問題種別と対処ステップ](#verifyadmin-の問題種別と対処ステップ)
- [ブランチ戦略と修正フロー](#ブランチ戦略と修正フロー)

---

## 基本的なレビューフロー

各プロンプト実行後、教員は以下の3つの判断を下します：

### 1. ✅ 承認（Approve）
成果物に問題がなく、次のステップに進める状態。

**対応:**
- プルリクエストを承認し、mainブランチにマージ
- 次のステップのプロンプトを実行

### 2. 🔄 修正要求（Request Changes）
成果物に修正が必要だが、ステップのやり直しは不要。

**対応:**
- 具体的な修正内容をコメントで指示
- エージェントまたは手動で修正を実施
- 同じブランチで修正をコミット
- 再度レビューを実施

### 3. ❌ 却下（Reject）
成果物の方向性が間違っており、ステップをやり直す必要がある。

**対応:**
- プルリクエストをクローズ
- ブランチを削除または破棄
- 入力ファイル（`agent-input/topics.md` など）を修正
- 同じプロンプトを再実行して新しいブランチで作業

---

## 各ステップのレビューと承認フロー

### 1. `/topics.admin` - トピック整理

**成果物:** `agent-input/topics.md` のクリーン版

**レビュー観点:**
- ノイズ（入力例、コメント）が適切に削除されているか
- トピックの記述が明確で具体的か
- 事前知識レベルと学習目標が整合しているか

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | 次のステップ（`/suggest-scenario.admin` または `/plan.admin`）へ進む |
| 🔄 修正要求 | mainブランチで直接修正し、再レビュー |
| ❌ 却下 | `agent-input/topics.md` を再編集し、`/topics.admin` を再実行 |

**注意:** `/topics.admin` はブランチを作成せず、mainブランチで直接作業します。

---

### 2. `/suggest-scenario.admin` - シナリオ案提案（Optional）

**成果物:** `agent-output/scenario0.md`, `scenario1.md`, `scenario2.md`

**レビュー観点:**
- 3つのシナリオが明確に異なるアプローチを提示しているか
- トピックの学習目標に沿ったシナリオになっているか
- 実装難易度が適切か

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | 1. 採用するシナリオを選択<br>2. `agent-input/topics.md` に採用シナリオのパスを追記<br>3. PRをマージして `/plan.admin` へ進む |
| 🔄 修正要求 | 特定のシナリオファイルを修正し、同じブランチで再コミット |
| ❌ 却下 | 1. ブランチ `feature/suggest-scenarios` を削除<br>2. `/suggest-scenario.admin` を再実行 |

---

### 3. `/plan.admin` - 課題プラン作成

**成果物:** `agent-output/plan.md`

**レビュー観点:**
- 選定された課題実施方式が適切か
- ユーザーストーリーが具体的で理解しやすいか
- テストシナリオのステージ構成が妥当か
- 点数配分が適切か
- チュートリアルの必要性判断が妥当か

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | PRをマージして次のステップ（`/tutorial.admin` または `/readme.admin`）へ進む |
| 🔄 修正要求 | 1. `agent-output/plan.md` を直接編集して修正<br>2. 同じブランチ `feature/make-a-plan` で再コミット<br>3. 再レビュー |
| ❌ 却下 | 1. ブランチ `feature/make-a-plan` を削除<br>2. `agent-input/topics.md` を見直し<br>3. 必要に応じて `/suggest-scenario.admin` から再実行<br>4. `/plan.admin` を再実行 |

**修正が必要な場合の具体例:**
- テストシナリオのステージ数を増減
- 点数配分の調整
- ユーザーストーリーの記述改善
- 実装対象ファイル名の変更

---

### 4. `/tutorial.admin` - チュートリアル作成

**成果物:** `TUTORIAL.md`

**レビュー観点:**
- プランで「必要」と判断された前提知識がカバーされているか
- 説明が初学者にとってわかりやすいか
- サンプルコードが適切か
- 段階的な説明になっているか

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | PRをマージして `/readme.admin` へ進む |
| 🔄 修正要求 | 1. `TUTORIAL.md` を直接編集<br>2. 同じブランチ `feature/make-tutorial` で再コミット<br>3. 再レビュー |
| ❌ 却下 | 1. ブランチ `feature/make-tutorial` を削除<br>2. `/tutorial.admin` を再実行 |

---

### 5. `/readme.admin` - README作成

**成果物:** `release/README.md`

**レビュー観点:**
- プランの内容が正確に反映されているか
- 学生にとって理解しやすい説明になっているか
- 実施手順が明確か
- 評価基準が明記されているか

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | PRをマージして `/implement-test.admin` へ進む |
| 🔄 修正要求 | 1. `release/README.md` を直接編集<br>2. 同じブランチ `feature/make-readme` で再コミット<br>3. 再レビュー |
| ❌ 却下 | 1. ブランチ `feature/make-readme` を削除<br>2. 必要に応じて `agent-output/plan.md` を修正<br>3. `/readme.admin` を再実行 |

---

### 6. `/implement-test.admin` - テスト実装と検証

**成果物:** `tests/` 配下のテストファイル、模範解答コード（`agent-output/` 内）

**レビュー観点:**
- テストがプランのテストシナリオを正確に実装しているか
- 各ステージのテストが適切に分離されているか
- テストが正しく動作するか（模範解答でGREEN、初期状態でRED）
- CI設定（`.github/workflows/classroom.yml`）が正しいか

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | PRをマージして `/verify.admin` へ進む |
| 🔄 修正要求 | 1. テストコードやCI設定を直接編集<br>2. 同じブランチ `feature/implement-tests` で再コミット<br>3. テストを再実行して動作確認<br>4. 再レビュー |
| ❌ 却下 | 1. ブランチ `feature/implement-tests` を削除<br>2. 必要に応じて `agent-output/plan.md` を修正<br>3. `/implement-test.admin` を再実行 |

**よくある修正内容:**
- テストケースの追加・削除
- テストの期待値修正
- ステージ分割の調整
- CI設定の点数配分修正

---

### 7. `/verify.admin` - 包括的検証

**成果物:** 修正されたファイル（問題が見つかった場合）

**レビュー観点:**
- 課題内容の明確性
- CIテストの妥当性
- 不要なファイルの有無
- 全体的な整合性

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認（問題なし） | PRをマージして `/release.admin` へ進む |
| 🔄 修正要求（軽微な問題） | 1. エージェントが提示した修正リストを確認<br>2. 承認後、同じブランチ `feature/verify-assignment` で修正<br>3. 再レビュー |
| ❌ 却下（重大な問題） | **下記「問題種別と対処ステップ」を参照** |

---

## `/verify.admin` の問題種別と対処ステップ

`/verify.admin` で発見される問題は、その性質に応じて適切なステップに戻る必要があります。

| 問題の種別 | 戻り先 | 対応手順 |
|-----------|--------|----------|
| **課題内容の不明瞭性** | `/readme.admin` | 1. ブランチ `feature/verify-assignment` を削除<br>2. `release/README.md` を直接修正、または `/readme.admin` を再実行<br>3. `/verify.admin` を再実行 |
| **テストシナリオの不整合** | `/plan.admin` | 1. ブランチ `feature/verify-assignment` を削除<br>2. `agent-output/plan.md` を修正<br>3. 必要に応じて `/readme.admin` と `/implement-test.admin` を再実行<br>4. `/verify.admin` を再実行 |
| **テスト実装の問題**<br>（テストが正しく動作しない、テストケース不足など） | `/implement-test.admin` | 1. ブランチ `feature/verify-assignment` を削除<br>2. テストファイルを直接修正、または `/implement-test.admin` を再実行<br>3. `/verify.admin` を再実行 |
| **CI設定の誤り**<br>（点数配分、ワークフロー設定など） | `/implement-test.admin` | 1. ブランチ `feature/verify-assignment` を削除<br>2. `.github/workflows/classroom.yml` を直接修正<br>3. `/verify.admin` を再実行 |
| **チュートリアルの不足/不備** | `/tutorial.admin` | 1. ブランチ `feature/verify-assignment` を削除<br>2. `TUTORIAL.md` を直接修正、または `/tutorial.admin` を再実行<br>3. `/verify.admin` を再実行 |
| **トピックレベルの問題**<br>（課題の方向性が根本的に不適切） | `/topics.admin` または `/plan.admin` | 1. すべての作業ブランチを削除<br>2. `agent-input/topics.md` を見直し<br>3. `/topics.admin` から全体をやり直し |
| **軽微な修正**<br>（タイポ、フォーマット、不要ファイル削除など） | 現在のステップで対応 | 1. 同じブランチ `feature/verify-assignment` で直接修正<br>2. コミット・プッシュ<br>3. PRをマージして `/release.admin` へ進む |

**判断基準:**
- **軽微な問題**: タイポ、コメント、不要ファイル、フォーマットなど → 現在のブランチで修正
- **中程度の問題**: テストの微調整、READMEの部分修正など → 該当ステップに戻る
- **重大な問題**: 課題の方向性、テストシナリオの大幅変更など → プランまたはトピックから見直し

---

## 8. `/release.admin` - リリース準備

**成果物:** リリース可能な状態のリポジトリ

**レビュー観点:**
- 開発用ファイルが適切に削除されているか
- `release/` 内のファイルが適切に移動されているか
- 学生向けの状態になっているか

**レビュー後の対応:**

| 判断 | 対応手順 |
|------|----------|
| ✅ 承認 | PRをマージして課題リポジトリ完成 |
| 🔄 修正要求 | 1. 削除/移動の漏れを修正<br>2. 同じブランチ `feature/remove-admin-prompts` で再コミット<br>3. 再レビュー |
| ❌ 却下 | **リリース前の最終段階のため、通常は発生しない**<br>重大な問題が見つかった場合は `/verify.admin` に戻る |

---

## ブランチ戦略と修正フロー

### ブランチ命名規則

各プロンプトは専用のブランチで作業を行います（`/topics.admin` を除く）：

| プロンプト | ブランチ名 |
|----------|-----------|
| `/topics.admin` | _(mainで直接作業)_ |
| `/suggest-scenario.admin` | `feature/suggest-scenarios` |
| `/plan.admin` | `feature/make-a-plan` |
| `/readme.admin` | `feature/make-readme` |
| `/tutorial.admin` | `feature/make-tutorial` |
| `/implement-test.admin` | `feature/implement-tests` |
| `/verify.admin` | `feature/verify-assignment` |
| `/release.admin` | `feature/remove-admin-prompts` |

### 修正時のブランチ戦略

#### ケース1: 軽微な修正（🔄 修正要求）

**状況:** レビューで軽微な修正が必要と判断された場合

**ブランチ戦略:**
```bash
# 現在のブランチで修正を継続
git checkout feature/make-a-plan  # 例: /plan.admin のブランチ
# ファイルを編集
git add .
git commit -m "fix: レビュー指摘事項の修正"
git push origin feature/make-a-plan
# 同じPRで再レビュー
```

**利点:**
- 履歴が追いやすい
- PRのコメントが維持される
- 再実行のオーバーヘッドがない

#### ケース2: 大幅な修正（❌ 却下）

**状況:** 方向性が間違っており、ステップをやり直す必要がある場合

**ブランチ戦略:**
```bash
# 現在のブランチを破棄
git checkout main
git branch -D feature/make-a-plan

# 入力を修正
# 例: agent-input/topics.md を編集

# プロンプトを再実行（新しいブランチが自動作成される）
# /plan.admin を実行
```

**利点:**
- クリーンな状態から再スタート
- 失敗した試みが履歴に残らない
- 方向性を大きく変更できる

#### ケース3: 前のステップに戻る必要がある場合

**状況:** `/verify.admin` で問題が見つかり、特定のステップに戻る必要がある場合

**ブランチ戦略:**
```bash
# 現在のブランチを保持（または削除）
git checkout main

# 対象ステップの入力/成果物を修正
# 例: agent-output/plan.md を編集

# 必要なステップを順次再実行
# /readme.admin
# /implement-test.admin
# /verify.admin

# または、該当ファイルを直接修正
git checkout feature/implement-tests  # 例: テスト修正が必要な場合
# テストファイルを編集
git add .
git commit -m "fix: /verify.admin で指摘された問題を修正"
git push origin feature/implement-tests
```

**判断基準:**
- **直接修正する場合**: 問題が明確で、修正内容が確実な場合
- **再実行する場合**: エージェントの支援が必要な場合、または大規模な変更が必要な場合

### マージ順序の重要性

ブランチは以下の順序でマージされる必要があります：

```
main
 └─ feature/suggest-scenarios (Optional) → main
     └─ feature/make-a-plan → main
         └─ feature/make-tutorial (Optional) → main
             └─ feature/make-readme → main
                 └─ feature/implement-tests → main
                     └─ feature/verify-assignment → main
                         └─ feature/remove-admin-prompts → main
```

**重要:** 
- 前のステップがマージされていない場合、次のステップは開始しないでください
- 前のステップに戻る場合、その後のすべてのステップを再実行する必要があります

---

## トラブルシューティング

### Q1: レビュー後、どの程度の修正なら同じブランチで対応すべきか？

**A:** 以下を目安にしてください：

**同じブランチで修正:**
- タイポや文言の微調整
- テストケースの追加・削除（数個程度）
- CI設定の点数配分調整
- 不要なファイルの削除
- コメントの追加・修正

**ブランチを破棄して再実行:**
- 課題の方向性の大幅な変更
- テストシナリオの大規模な再構成
- ユーザーストーリーの全面的な書き直し
- 課題実施方式の変更

### Q2: `/verify.admin` で複数の問題が見つかった場合は？

**A:** 問題の種別に応じて優先順位を決定します：

1. **最も根本的な問題から対処**: トピックレベル > プランレベル > 実装レベル
2. **複数の軽微な問題**: 同じブランチでまとめて修正可能
3. **複数の重大な問題**: 最も上流のステップに戻り、順次やり直し

**例:**
- プランの問題 + テストの問題 → `/plan.admin` に戻る（テストも再実装される）
- READMEの問題 + CIの問題 → それぞれ直接修正可能

### Q3: 手動修正とプロンプト再実行、どちらを選ぶべきか？

**A:** 以下の基準で判断してください：

**手動修正を推奨:**
- 修正内容が明確で小規模
- 具体的な修正箇所がわかっている
- エージェントの出力形式を維持したい

**プロンプト再実行を推奨:**
- 修正内容が不明確または広範囲
- エージェントの支援が必要
- 全体的な一貫性を保ちたい

### Q4: マージ後に問題が見つかった場合は？

**A:** 既にmainにマージされたステップで問題が見つかった場合：

1. **軽微な問題**: mainブランチで直接修正してコミット
2. **中程度の問題**: 該当ステップのプロンプトを再実行し、新しいブランチで作業
3. **重大な問題**: `/verify.admin` を実行して包括的に再チェック

---

## まとめ

- 各ステップのレビューでは **承認/修正要求/却下** のいずれかを判断
- 軽微な修正は **同じブランチ** で対応
- 大幅な変更が必要な場合は **ブランチを破棄して再実行**
- `/verify.admin` の問題は **種別に応じて適切なステップに戻る**
- ブランチは **順序を守ってマージ**
- 不明な場合は **保守的に再実行** を選択

このフローに従うことで、効率的かつ確実に高品質な課題リポジトリを作成できます。
